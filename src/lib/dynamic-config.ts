// Dynamic configuration for pages and components that need dynamic rendering
export const dynamicConfig = {
  forceDynamic: true
}

// Helper to ensure dynamic rendering
export const ensureDynamic = () => {
  // This function helps ensure dynamic rendering is enforced
  return true
}
